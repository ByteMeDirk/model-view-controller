context:
  environment: dev
connection:
    type: postgres
    host: localhost
    port: 5432
    user: postgres
    password: postgres
    database: postgres
schemas:
  - name: public
    description: Default schema
  - name: customer
    description: Customer schema
  - name: product
    description: Product schema