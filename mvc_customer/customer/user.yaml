name: customer
description: Customer table
schema: {{ "dev_" if environment == "dev" else "" }}customer
columns:
  - name: id
    type: int
    primary_key: true
  - name: name
    type: varchar
  - name: email
    type: varchar
  - name: dob
    type: date